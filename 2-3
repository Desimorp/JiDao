#include<stdio.h>
#include<math.h>

int main() {
	double a, b, c, d;
	scanf("%lf %lf %lf %lf", &a, &b, &c, &d);
	if ((fabs(b * c - d) - 0) < 1.0E-10)
		printf("error");
	else
		printf("%.1lf", a / (b * c - d));
	return 0;
}
