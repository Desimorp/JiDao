#include<stdio.h>

int main(){
    double a[100];
    int n;
    double avg = 0;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%lf",&a[i]);
        avg += a[i];
    }
    printf("%.2lf\n",avg/n);
    return 0;
}
