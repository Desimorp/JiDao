#include <stdio.h>

int main() 
{
    char x;
    scanf("%c", &x);
    while (x != '\n')
    {
        if (x == 'u')
        {
            char y;
            scanf("%c", &y);
            if (y == 'i')
            {
                char z;
                scanf("%c", &z);
                if (z == 'd')
                {
                    char w;
                    scanf("%c", &w);
                    if (w == '=')
                    {
                        char t;
                        scanf("%c", &t);
                        while (t >= '0' && t <= '9')
                        {
                            printf("%c", t);
                            scanf("%c", &t);
                        }
                    }
                }
            }
        }
        scanf("%c", &x);
    }
    return 0;
}
