#include <stdio.h>

int main() 
{
    int t, s;
    s = -1;
    scanf("%d", &t);
    while (t != 0)
    {
        if ((s > t || s == -1)&& t > 0)//启动s，不断比较变换 
            s = t;
        else
            ;
        scanf("%d", &t);
    }
    printf("%d", s);
    return 0;
}
