#include<stdio.h>

int main() {
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		int t;
		int s = 0;
		scanf("%d", &t);
		while (t != -1) {
			if (t % 7 == 3 && t % 5 != 0)
				s += t;
			scanf("%d", &t);
		}
		printf("%d\n", s);
	}
	return 0;
}
