#include<stdio.h>
int edg(int x,int y){
    int sum = 0;
    while(x!=0){
        int t = 0;
        sum += (x % 10) * (x % 10); 
        x/=10;
    }
    return sum;
}
int main(){
    int n, m;
    scanf("%d %d",&n,&m);
    for (int i = 1; i <= n; i++)
    {
        if(i / m == edg(i,m))
            printf("%d\n", i);
    }
    return 0;
}
