#include<stdio.h>
#define MAX 100
void Input(int a[][MAX], int n, int m){
	for(int i=0;i<n;i++){
		for(int j=0;j<m;j++){
			scanf("%d", &a[i][j]);
		}
	}

}

void Output(int a[][MAX], int n, int m){
	for(int i=0;i<n;i++){
		for(int j=0;j<m;j++){
			printf("%d", a[i][j]);
			if(j == m - 1)
				printf("\n");
			else
				printf(" ");
		}
	}
}
void Transpose(int a[][MAX], int n, int m){
	int Tran[MAX][MAX];
	for(int i=0;i<n;i++){
		for(int j=0;j<m;j++){
			Tran[j][i] = a[i][j];
		}
	}
	Output(Tran, m, n);
}

void Operate(int a[][MAX], int n, int m){
	Input(a, n, m);
	Transpose(a, n, m);
}
int main(){
	int a[MAX][MAX], n, m;
	scanf("%d %d", &n, &m);
	Operate(a, n, m);
	return 0;
}
