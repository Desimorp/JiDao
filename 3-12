#include<stdio.h>

int main() {
	int m, n;
	scanf("%d %d", &m, &n);
	for (int i = 0; i <= m; i++)
	{
		for (int j = 0; j <= m; j++)
		{
			for (int k = 0; k <= m; k++)
			{
				if ((i * 1 + j * 2 + k * 3 == n - m) && (i + j + k <= m))
					printf("%d %d %d\n", i, j, k);
			}
		}
	}
	return 0;
}
