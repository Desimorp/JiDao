#include <stdio.h>

int main() 
{
    int t, s = 0;
    scanf("%d", &t);
    while (t != -1)
    {
        if (t < 80)
            s++;
        else
            ;
        scanf("%d", &t);
    }
    printf("%d", s);
    return 0;
}
