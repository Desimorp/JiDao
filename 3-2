#include<stdio.h>

int main()
{
	char t;
	scanf("%c", &t);
	while (t != '\n')
	{
		if (t <= 'Z')
			printf("%c", 'A' + (t - 'A' + 3) % 26);
		else
			printf("%c", 'a' + (t - 'a' + 3) % 26);
		scanf("%c", &t);
	}
	return 0;
}
