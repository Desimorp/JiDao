#include<stdio.h>

int main() {
	float a, b, c;
	scanf("%f %f %f", &a, &b, &c);
	printf("%.1f\n", 0.3 * a + 0.1 * b + 0.6 * c);
	if (0.3 * a + 0.1 * b + 0.6 * c < 60)
		printf("failed");
	else
		printf("pass");
	return 0;
}
