#include<stdio.h>

int main()
{
	int n, sum = 0;
	int max = 1, min = -1;
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		int t;
		scanf("%d", &t);
		sum += t;
		if (t > max || max == 1)
			max = t;
		if (t < min || min == -1)
			min = t;
	}
	printf("%d %d %d", sum, max, min);
	return 0;
}
