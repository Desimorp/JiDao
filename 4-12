#include<stdio.h>
#include<math.h>

int sum(int x, int y);
void print(int* ans, int n);

int main()
{
    int n = 0;
    int ans[10000];
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    for (int i = a; i <= b; i++)
    {
        if(sum(i,c))
        {
            ans[n] = i;
            n++;
        }
    }
    print(ans,n);
    return 0;
}

int sum(int x, int y)
{
    int sum = 0;
    for (int i = 1; i < x; i++)
    {
        if(!(x%i))
            sum += i;
    }
    if(fabs(sum-x)<=y)
        return 1;
    else
        return 0;
}

void print(int* ans, int n)
{
    if(n)
    {    
        for (int i = 0; i < n-1; i++)
        {
            printf("%d",ans[i]);
            if(!((i+1)%5))
                printf("\n");
            else
                printf("\t");
        }
        printf("%d\n",ans[n-1]);
    }
    else
        printf("There is no proper number in the interval.\n");    
    return;
}
